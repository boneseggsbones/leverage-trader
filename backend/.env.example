# Backend Environment Variables

# PriceCharting API Token (40 characters)
# Get your token from: https://www.pricecharting.com/subscriptions
# Requires a paid PriceCharting Pro subscription (~$50/month)
PRICECHARTING_API_TOKEN=your_40_character_token_here

# eBay Marketplace Insights API
# Register at: https://developer.ebay.com
# Create an application to get App ID and Cert ID
# Marketplace Insights API is in beta - may require additional access approval
EBAY_APP_ID=your_ebay_app_id
EBAY_CERT_ID=your_ebay_cert_id
EBAY_ENVIRONMENT=SANDBOX

# eBay User OAuth (for inventory import)
# Requires Sell API access (sell.inventory scope)
# EBAY_RU_NAME: Your registered Redirect URI name in eBay developer console
EBAY_REDIRECT_URI=http://localhost:3000/import/ebay
EBAY_RU_NAME=

# Auth.js Configuration
# AUTH_SECRET: Generate with `openssl rand -base64 32`
AUTH_SECRET=your-secret-key-change-in-production

# Google OAuth (https://console.cloud.google.com/apis/credentials)
# Create OAuth 2.0 Client ID (Web application)
# Authorized redirect URI: http://localhost:4000/api/auth/callback/google
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Email Notifications (SMTP)
# For Gmail: Use App Password (https://myaccount.google.com/apppasswords)
# For SendGrid: Use SendGrid SMTP settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM="Leverage" <noreply@leverage.app>
APP_URL=http://localhost:3000
